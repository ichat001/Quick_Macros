# 触发器：进程

## 概述

当指定的进程（程序）启动或结束时触发宏的执行。可以使用属性对话框分配此触发器。在[便携式 QM](IDP_PORTABLE.md)中不可用。

## 功能说明

- **“Already running”选项**：如果选中“Already running”，当 QM 启动或添加、移除、修改进程触发器时，如果指定进程已在运行，将触发宏。

- **触发信息**：宏可以通过在关闭属性对话框时经确认插入的[函数](IDP_FUNCTION.md)语句接收触发信息。